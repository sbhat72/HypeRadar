# Env files — never commit secrets
.env
.env.local
.env*.local
frontend/.env.local
backend/.env

# Local Spring Boot config — contains real secrets, never commit
**/application-local.properties
**/application-default.properties

# Java build output
backend/target/

# Node
frontend/node_modules/
frontend/.next/
frontend/tsconfig.tsbuildinfo
frontend/next-env.d.ts

# Throwaway local scaffolding, not part of the product
clerk-nextjs/
